## 解题思路
* 第一种方式，最简单就是遍历所有情况，但是这样会超时
* 采用双边指针，从数组两边开始遍历，此处需要应用一个结论来判断左、右指针哪个先移动
  * 即先移动指针对应的高度小的一侧的指针，因为如果先移动高度大的一侧，x轴的长度减少了，但是y轴的高度最多也只能用高度小的一侧，
  所以此时移动高度大的一侧，不会更好，但是移动高度小的一侧，不会更差，所以博弈之后结论是移动高度小的一侧。