## 解题思路：
> 前提：题中元素不允许出现重复，并且只存在一对符合条件的 
* 最简单的办法就是通过两重for循环进行搜索 ，但是两重循环的时间复杂度为O(N^2), 所以比较耗时。
* 另外的方式，就是通过hash的方式进行，即定义一个hash表，其中的key为target - 当前数组的值， value则是当前数据的值对应的index，当遇见符合条件的值，直接返回hash表中的index和当前的index，这样的时间复杂度为O(N), 由于使用了hash表，所以空间复杂度为O(N).
